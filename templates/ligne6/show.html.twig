{% extends 'base.html.twig' %}

{% block title %}Ligne6{% endblock %}

{% block body %}
    <h1>Ligne6</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligne6.id }}</td>
            </tr>
            <tr>
                <th>Trajet</th>
                <td>{{ ligne6.Trajet }}</td>
            </tr>
            <tr>
                <th>Tarif</th>
                <td>{{ ligne6.Tarif }}</td>
            </tr>
            <tr>
                <th>Disposition</th>
                <td>{{ ligne6.Disposition ? ligne6.Disposition|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ligne6_index') }}">back to list</a>

    <a href="{{ path('app_ligne6_edit', {'id': ligne6.id}) }}">edit</a>

    {{ include('ligne6/_delete_form.html.twig') }}
{% endblock %}
